import { PropsWithData } from "@comet/cms-site";

import { NewsletterImageBlockData } from "../blocks.generated";

export const NewsletterImageBlock = ({ data }: PropsWithData<NewsletterImageBlockData>) => {
    return null;
};
